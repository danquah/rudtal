apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Platform controllers are version-pinned and reconciled before their dependent
# configuration. Credential data is never committed in these directories.
resources:
  - cilium
  - tailscale
